# Tools
CURL_BIN=/usr/bin/curl
UNZIP_BIN=unzip
JAR_BIN=jar

CRX_UPLOAD_PATH=/crx/packmgr/service/.json/etc/packages
CRX_INSTALL_PREFIX=/crx/packmgr/service/.json
PKG_BASE_PATH=/etc/packages
EXPLODE_ROOT=/var/lib/build/explode
DEFAULT_AUTHOR_PORT=4502
DEFAULT_PUBLISH_PORT=4503

# Credentials (DO NOT commit real secrets in repo; replace for test if needed)
aem_build_user='build-admin:bU!ld@dm1n'

# Environment tokens and mappings
ENV_TOKEN_dev=dev
ENV_TOKEN_stage=stg
ENV_TOKEN_prod=prd

# Example server entries (replace with real)
dev_dam_aem_authors=dev-author.example.com:4502
dev_dam_aem_publishers=dev-pub.example.com:4503

# Build flags
dev_build_allowed=true
stage_build_allowed=true
production_build_allowed=false
